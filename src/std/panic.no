pub let panic = fn(message: String): Never {
    throwError("panic: {message}")
}

pub let todo = fn(message: String): Never {
    panic("todo: {message}")
}

let throwError = fn(message: String): Never
