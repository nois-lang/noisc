trait Iterable<T> {
    fn iter(self): Iter<T>
}

trait Iter<T> {
    fn next(self): Option<T>

    fn collect<C: Collector<T>>(self): C {
        C::fromIter(self)
    }
}

trait Collector<T> {
    fn fromIter<I: Iter<T>>(iter: I): Self
}
