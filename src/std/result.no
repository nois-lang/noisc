pub type Result<T, E> {
    Ok(pub value: T),
    Error(pub error: E)
}

impl <T, E> Result<T, E> {
    pub fn isOk(self): Bool {
        match self {
            Ok() { true }
            _ { false }
        }
    }
    pub fn isError(self): Bool {
        match self {
            Error() { true }
            _ { false }
        }
    }
}
